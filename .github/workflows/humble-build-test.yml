name: Humble Build and Test

on:
  push:
  pull_request:
  schedule:
    - cron: "0 5 * * 1"

jobs:
  humble_build_and_test:
    runs-on: ubuntu-latest
    container:
      image: ros:humble
    steps:
      - name: Build and Test
        uses: ros-tooling/action-ros-ci@v0.4
        with:
          package-name: yasmin yasmin_ros yasmin_factory
          target-ros2-distro: humble
          coverage-result: false
